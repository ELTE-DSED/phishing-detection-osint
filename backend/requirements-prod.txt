# Production Dependencies
# ======================
# Only packages needed to run the API server.
# Training/evaluation packages (matplotlib, seaborn, optuna, shap, tqdm)
# and test packages (pytest, etc.) are excluded for faster deploys.

# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# OSINT Data Collection
python-whois==0.8.0
dnspython==2.5.0
requests==2.31.0
aiohttp==3.9.1
httpx==0.26.0

# NLP & Machine Learning (runtime only)
spacy==3.7.2
scikit-learn==1.4.0
xgboost>=2.0.0
numpy>=1.26.0
joblib>=1.3.0
pandas>=2.1.0

# Configuration & Environment
python-dotenv==1.0.0

# Async Support
asyncio-throttle==1.0.2

# Logging & Monitoring
structlog==24.1.0
